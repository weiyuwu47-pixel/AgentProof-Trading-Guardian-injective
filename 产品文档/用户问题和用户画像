# 用户问题与用户画像


> 项目阶段：Hackathon MVP

---

## 1. 文档目的

本文档用于明确芙副官的目标用户、核心问题、典型使用场景与用户旅程。


---

## 2. 背景假设

盯盘 Agent ,策略agent等正在变得越来越聪明。但用户不敢用。
金融场景是重要的高风险场景。即使 AI Agent 能给出看似合理的建议，用户仍然会面临明显的信任障碍：

* AI 可能幻觉市场数据；
* AI 可能忽略用户设置的策略；
* AI 可能违反风控规则；
* AI 可能使用未授权输入；
* AI 可能生成无法审计的决策过程。

因此，AI 金融 Agent 不只需要“能生成建议”，还需要“能证明自己是如何生成建议的”。

在未来agent to agent的环境中，金融agent彼此相互协作。比如风控agent、保险agent、盯盘agent等。彼此需要一个证明

---

## 3. 核心用户问题

### 3.1 用户表层问题

用户希望 AI Agent 帮自己盯盘，减少手动看盘成本。

例如：

> “我有我自己的仓位管理、交易策略，能不能让 AI 帮我看盘，出现信号时提醒我？”

这是用户表层需求。

---

### 3.2 用户深层问题

用户真正担心的不是 AI 是否能提醒，而是：

> “这个提醒可信吗？”

具体包括：

* 这个 Agent 真的读取了实时 / 记录中的行情吗？
* 它有没有编造价格、成交量或突破信号？
* 它有没有遵守我设置的交易规则？
* 它有没有违反我的仓位限制？
* 我收到的提醒是否可以被第三方验证？
* 如果多个 Agent 未来协作，谁该对某一步决策负责？

---

### 3.3 项目要解决的问题

芙副官解决的是：

> AI 金融 Agent 的决策过程不可验证问题。

它要解决：

> 如何证明 Agent 在给出建议时是否遵守了用户策略、使用了授权数据、通过了风控检查，并将 proof 锚定到链上。

---

## 4. 目标用户

### 4.1 用户类型一：个人投资者

#### 用户描述

已经开始使用 AI 工具辅助投资、盯盘或复盘的个人用户。

#### 典型特征

* 有自己的交易规则或仓位习惯；
* 希望减少手动盯盘压力；
* 对 AI 工具感兴趣，但不愿完全信任 AI；
* 更愿意接受“提醒 + 验证”，而不是“自动交易”。

#### 核心需求

* 根据自己的策略获得提醒；
* 知道 Agent 为什么给出建议；
* 知道建议是否违反自己的仓位限制；
* 收到带证明的可验证提示。

---

### 4.2 用户类型二：AI Trading Tool / Bot 开发者

#### 用户描述

正在构建 AI 盯盘工具、AI trading assistant 或策略提醒工具的开发者 / 小团队。

#### 典型特征

* 关注 AI 生成建议的体验；
* 希望提高用户对 AI 决策的信任；
* 需要向用户解释 Agent 的行为；
* 可能计划未来接入链上交易或钱包能力。

#### 核心需求

* 给自己的 AI Agent 增加可验证能力；
* 记录 Agent 输入、输出和行为过程；
* 在关键决策中生成 proof；
* 降低用户对 AI 幻觉的担忧。

#### 核心顾虑

* 用户不相信 AI 推荐；
* 出现错误建议后难以解释；
* 决策过程缺乏审计记录；
* 未来接入交易执行时风险更高。

---

### 4.3 用户类型三：钱包 / 交易平台 / DeFi 协议

#### 用户描述

希望接入 AI Agent 能力的钱包、交易平台或 DeFi 协议方。

#### 典型特征

* 已有金融交易或资产管理场景；
* 可能正在探索 AI Agent 作为用户助手；
* 需要降低 AI 自动化带来的信任风险；
* 对链上记录、可追溯性、审计能力有更高要求。

#### 核心需求

* 验证 AI Agent 决策是否合规；
* 追踪 Agent 在交易前使用了哪些数据；
* 区分 Agent 身份和行为记录；
* 为未来多 Agent 协作提供审计基础。

#### 核心顾虑

* AI Agent 误导用户；
* Agent 越权读取或执行；
* 出现争议时缺少决策证据；
* 单纯交易 hash 无法解释 AI 决策过程。

---

## 6. 典型使用场景

### 场景一：用户设置自然语言交易策略

用户输入类似策略：

```text
MA5 / MA10 下方不追涨；
只有放量突破关键价位才允许买；
跌破止损价位才允许卖；
保留底仓，不允许清仓；
没有明确信号时保持不动。
```

系统需要将该策略作为 Agent 决策约束，并在后续生成 `strategyHash`。

---

### 场景二：Agent 读取市场数据并生成“保持不动”决策

Agent 读取中国移动的行情数据后，发现市场没有满足买入条件，或虽然出现卖出信号但卖出会违反最低底仓规则。

因此 Agent 输出：

```text
Decision: HOLD
Reason: 当前决策遵守用户底仓限制，避免违反风控规则。
```

系统生成 behavior log、proof metadata 和 proof hash。

---

### 场景三：用户收到可验证提示
用户收到的不是普通提醒：

```text
中国移动：HOLD
```

而是 verified alert：

```text
中国移动：HOLD
Verification: PASSED
Proof: Anchored on Injective Testnet
```

用户可以进一步查看：

* Agent 身份；
* 策略依据；
* 行情数据；
* 行为日志；
* proof hash；
* Injective tx hash；
* verifier 检查结果。

---

### 场景四：Agent 出现幻觉时被拒绝

Agent 幻觉声称：

```text
价格突破 112，建议 BUY。
```

但记录中的市场最高价并没有达到 112。

此时 verifier 检查失败，并拒绝该决策：

```text
Verification: REJECTED
Reason: claimed breakout is not supported by recorded market data.
```

该场景用于说明：

> AgentProof 不依赖 AI 自己声称诚实，而是用记录数据、策略、行为日志和 hash 重新验证输出。

---

### 场景五：未来多 Agent 协作

未来金融 Agent 可能不是单个 Agent 工作，而是多个 Agent 共同完成任务：

```text
Market Data Agent → 提供市场数据
Strategy Agent → 读取用户策略
Risk Guardian Agent → 检查仓位和风险
Portfolio Agent → 管理资产配置
Execution Agent → 执行链上交易
Audit Agent → 验证完整决策链路
```

在这种场景下，用户需要知道：

* 每个 Agent 是谁；
* 每个 Agent 负责什么；
* 每个 Agent 输出了什么；
* 哪些输出被后续 Agent 使用；
* 最终决策是否可以追溯和验证。

AgentProof 的长期价值是为多 Agent 金融工作流提供行为证明和审计基础。

---

## 7. 用户旅程

### 7.1 当前 MVP 用户旅程

```text
用户设置交易策略
        ↓
Agent 读取市场数据
        ↓
Agent 生成 BUY / SELL / HOLD 决策
        ↓
系统记录行为日志
        ↓
系统生成 proof metadata
        ↓
系统生成 proof hash
        ↓
proof hash 锚定到 Injective testnet
        ↓
verifier 重新计算并验证
        ↓
用户收到 verified alert
```

---

---

## 8. 用户痛点优先级

| 优先级 | 痛点             | 说明                      |
| --- | -------------- | ----------------------- |
| P0  | AI 幻觉行情        | 金融场景中，错误行情会直接影响用户判断     |
| P0  | 忽略用户策略         | 用户不信任不按规则执行的 Agent      |
| P0  | 违反仓位 / 风控限制    | 涉及真实资金风险，是核心安全边界        |
| P1  | 决策过程不可追溯       | 用户无法理解 Agent 为什么给出建议    |
| P1  | 普通提醒缺乏可信证明     | 用户收到提醒但不知道是否可信          |
| P2  | 多 Agent 协作责任不清 | 未来复杂金融 Agent 工作流需要解决的问题 |

---

## 9. 用户需求拆解

### 9.1 功能性需求

* 用户可以定义交易策略；
* 系统可以读取市场数据；
* Agent 可以生成交易决策；
* 系统可以记录 Agent 行为；
* 系统可以生成 proof hash；
* proof hash 可以锚定到 Injective；
* verifier 可以重新验证 proof；
* 系统可以展示 hallucination detection negative case；
* 用户可以看到 verified alert。

---

### 9.2 非功能性需求

* 决策过程应可追溯；
* proof 生成过程应稳定；
* hash 计算应避免字段顺序影响；
* demo 应可稳定运行；
* 不应暴露 private key；
* 不应承诺收益；
* 不应假装已完成未实现的官方 Agent Registry 集成。

---

## 10. 本项目的用户价值总结

AgentProof Trading Guardian 的用户价值不在于替用户预测市场，而在于降低用户使用 AI 金融 Agent 时的信任成本。

它让用户从：

```text
“AI 建议我 HOLD，但我不知道它有没有乱说。”
```

转变为：

```text
“AI 建议我 HOLD，我可以查看它用了什么数据、遵守了什么策略、通过了哪些验证，以及 proof 是否已经锚定到 Injective。”
```

---

## 11. 结论

对于 AI 金融 Agent 来说，信任不是一句“AI 很聪明”就能建立的。

尤其在交易、资产管理、钱包和 DeFi 场景中，用户需要的不只是更强的 Agent，而是更可验证的 Agent。

AgentProof Trading Guardian 因此选择从盯盘 Agent 切入，先验证单 Agent 的决策过程，再逐步扩展到多 Agent 金融工作流。

当前 MVP 的核心用户价值是：

> 让 AI 金融 Agent 的每一次关键决策，都可以被记录、哈希、锚定和验证。
